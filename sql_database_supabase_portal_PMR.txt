-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.users_profile (
  id uuid NOT NULL,
  email text NOT NULL UNIQUE,
  nama_lengkap text NOT NULL,
  nama_panggilan text NOT NULL,
  tanggal_lahir date NOT NULL,
  kelas text NOT NULL,
  nisn text,
  jabatan text NOT NULL DEFAULT 'non-aktif'::text CHECK (jabatan = ANY (ARRAY['admin'::text, 'pengurus'::text, 'anggota'::text, 'non-aktif'::text])),
  keterangan_jabatan text NOT NULL CHECK (keterangan_jabatan = ANY (ARRAY['Kepala Sekolah'::text, 'Pembina 1'::text, 'Pembina 2'::text, 'Pelatih'::text, 'Ketua'::text, 'Wakil'::text, 'Sekretaris'::text, 'Bendahara'::text, 'Seksi'::text, 'Anggota'::text, 'Alumni'::text, 'Non-Aktif'::text])),
  no_wa_pribadi text,
  no_wa_ortu text,
  foto_profil_url text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  golongan_darah text CHECK (golongan_darah = ANY (ARRAY['A'::text, 'B'::text, 'AB'::text, 'O'::text])),
  rhesus_darah text CHECK (rhesus_darah = ANY (ARRAY['Positif'::text, 'Negatif'::text])),
  riwayat_penyakit text,
  tahun_bergabung character varying,
  jenis_kelamin character varying CHECK (jenis_kelamin::text = ANY (ARRAY['Laki-laki'::character varying, 'Perempuan'::character varying]::text[])),
  CONSTRAINT users_profile_pkey PRIMARY KEY (id),
  CONSTRAINT users_profile_id_fkey FOREIGN KEY (id) REFERENCES auth.users(id)
);
CREATE TABLE public.presensi (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  sesi_id uuid NOT NULL,
  user_id uuid NOT NULL,
  nama_kegiatan text NOT NULL,
  jenis_kegiatan text NOT NULL CHECK (jenis_kegiatan = ANY (ARRAY['Latihan Rutin'::text, 'Tugas Upacara'::text, 'Bakti Sosial'::text, 'Lomba PMR'::text, 'Diklat / Seminar'::text, 'Rapat Organisasi'::text, 'Lainnya'::text])),
  tempat_kegiatan text NOT NULL,
  deskripsi_kegiatan text,
  foto_dokumentasi_url text,
  status_kehadiran text NOT NULL CHECK (status_kehadiran = ANY (ARRAY['Hadir'::text, 'Izin'::text, 'Sakit'::text, 'Alpa'::text, 'Tidak Ditugaskan'::text])),
  tanggal_kegiatan date NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT presensi_pkey PRIMARY KEY (id),
  CONSTRAINT presensi_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users_profile(id)
);
CREATE TABLE public.inventaris_barang (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  kode_barang character varying UNIQUE,
  nama_barang character varying NOT NULL,
  kategori character varying NOT NULL,
  kepemilikan USER-DEFINED NOT NULL,
  nama_pemilik_pribadi character varying,
  jumlah integer NOT NULL DEFAULT 1 CHECK (jumlah >= 0),
  satuan character varying NOT NULL,
  lokasi_rak character varying NOT NULL,
  kondisi USER-DEFINED NOT NULL DEFAULT 'Baik'::kondisi_barang_lengkap,
  foto_barang_url text,
  tanggal_kedaluwarsa date,
  keterangan_catatan text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT inventaris_barang_pkey PRIMARY KEY (id)
);
CREATE TABLE public.inventaris_usulan (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nama_barang character varying NOT NULL,
  jumlah_diusulkan integer NOT NULL DEFAULT 1,
  alasan_kebutuhan text NOT NULL,
  pengusul_id uuid,
  status USER-DEFINED NOT NULL DEFAULT 'Menunggu'::status_usulan,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT inventaris_usulan_pkey PRIMARY KEY (id),
  CONSTRAINT inventaris_usulan_pengusul_id_fkey FOREIGN KEY (pengusul_id) REFERENCES auth.users(id)
);
CREATE TABLE public.inventaris_log_aktivitas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  barang_id uuid,
  nama_barang character varying NOT NULL,
  kepemilikan USER-DEFINED NOT NULL,
  aksi character varying NOT NULL,
  rincian_perubahan jsonb NOT NULL,
  petugas_id uuid NOT NULL,
  nama_petugas character varying NOT NULL,
  jabatan_petugas character varying NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT inventaris_log_aktivitas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.profil_ruang_uks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nama_ruangan character varying NOT NULL DEFAULT 'Ruang Utama UKS SPADAN'::character varying,
  lokasi_lantai character varying NOT NULL DEFAULT 'Lantai 1'::character varying,
  panjang_meter numeric NOT NULL DEFAULT 7.00,
  lebar_meter numeric NOT NULL DEFAULT 6.00,
  luas_total_m2 numeric DEFAULT (panjang_meter * lebar_meter),
  kapasitas_tempat_tidur integer NOT NULL DEFAULT 2,
  kondisi_lantai USER-DEFINED NOT NULL DEFAULT 'Baik'::kondisi_barang_lengkap,
  kondisi_dinding USER-DEFINED NOT NULL DEFAULT 'Baik'::kondisi_barang_lengkap,
  kondisi_plafon USER-DEFINED NOT NULL DEFAULT 'Baik'::kondisi_barang_lengkap,
  kondisi_pintu_jendela USER-DEFINED NOT NULL DEFAULT 'Baik'::kondisi_barang_lengkap,
  ada_toilet_dalam boolean DEFAULT false,
  ada_wastafel_air_mengalir boolean DEFAULT true,
  ada_pemisah_gender boolean DEFAULT true,
  ventilasi_udara character varying DEFAULT 'Baik / Memadai'::character varying,
  pencahayaan character varying DEFAULT 'Terang / Cukup'::character varying,
  foto_ruangan_url text,
  catatan_pemeliharaan text,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT profil_ruang_uks_pkey PRIMARY KEY (id)
);
CREATE TABLE public.utilitas_eksternal (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nama character varying NOT NULL,
  target_url text NOT NULL,
  ikon_url text NOT NULL,
  warna_bg character varying DEFAULT '#f8fafc'::character varying,
  warna_aksen character varying DEFAULT '#475569'::character varying,
  akses_minimal character varying DEFAULT 'guest'::character varying,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT utilitas_eksternal_pkey PRIMARY KEY (id)
);